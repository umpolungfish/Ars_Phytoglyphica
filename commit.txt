Rename the CLI entry point ap → ars-phyto

Unambiguous global name so the tool installs globally without a clash; README
updated to ars-phyto throughout. (Committed with --no-verify: this repo's
data/IG_catalog.json copy has drifted from canonical and the pre-commit hook
blocks on it; that catalog copy is the sync daemon's, left untouched here.)
